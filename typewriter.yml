# Segment Typewriter Configuration (https://segment.com/docs/protocols/typewriter)
# Just run `npx typewriter@next` to re-generate a client with the latest versions of these events.
scripts:
  # You can supply a Segment API token using a `script.token` command. See `Token Script` below.
  token: source ./env/.env.local; echo $SEGMENT_TYPEWRITER_TOKEN
  # You can format any of Typewriter's auto-generated files using a `script.after` command.
  # See `Formatting Generated Files` below.
  after: ./node_modules/.bin/prettier --write src/analytics/plan.json

# You can find more documentation on configuring this client in the Segment docs.
# See: https://segment.com/docs/protocols/typewriter
client:
  sdk: analytics.js
  language: typescript

trackingPlans:
  # Tracking Plan: P1 Events
  # https://app.segment.com/guildeducation/protocols/tracking-plans/rs_2IMbFgW8hRhJf0ucnhCwFRPayY2
  - id: rs_2IMbFgW8hRhJf0ucnhCwFRPayY2
    workspaceSlug: guildeducation
    path: src/analytics
